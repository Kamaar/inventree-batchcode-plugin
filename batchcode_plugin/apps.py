"""Django config for the BatchCodePlugin plugin."""

from django.apps import AppConfig


class BatchCodePluginConfig(AppConfig):
    """Config class for the BatchCodePlugin plugin."""

    name = 'batchcode_plugin'
    verbose_name = 'Batch Code Generator'
